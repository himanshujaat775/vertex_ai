<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>VertexAI — AI Employees for Every Industry</title>
  <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --ink: #0D0D0F; --slate: #1A1A2E; --indigo: #4F46E5; --violet: #7C3AED;
      --glow: #A78BFA; --soft: #F5F4FF; --muted: #6B7280; --white: #FFFFFF;
      --card-bg: #FAFAFA; --border: #E5E7EB; --success: #22C55E;
    }
    html { scroll-behavior: smooth; }
    body { font-family: 'Inter', sans-serif; background: var(--white); color: var(--ink); line-height: 1.6; }

    /* NAV */
    nav {
      position: sticky; top: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 18px 6%; background: rgba(255,255,255,0.93);
      backdrop-filter: blur(14px); border-bottom: 1px solid var(--border);
    }
    .nav-logo { font-family:'Syne',sans-serif; font-size:1.35rem; font-weight:800; color:var(--slate); text-decoration:none; }
    .nav-logo span { color: var(--indigo); }
    .nav-links { display:flex; gap:32px; list-style:none; }
    .nav-links a { font-size:.9rem; font-weight:500; color:var(--muted); text-decoration:none; transition:color .2s; }
    .nav-links a:hover { color:var(--indigo); }
    .nav-cta { background:var(--indigo); color:var(--white); padding:10px 22px; border-radius:8px; font-size:.88rem; font-weight:600; text-decoration:none; transition:background .2s; }
    .nav-cta:hover { background:var(--violet); }
    @media(max-width:768px){ .nav-links { display:none; } }

    /* HERO */
    .hero {
      padding: 90px 6% 70px;
      display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center;
      background: linear-gradient(135deg,#F5F4FF 0%,#EDE9FE 50%,#F0F9FF 100%);
    }
    @media(max-width:900px){ .hero { grid-template-columns:1fr; } .hero-visual { display:none; } }
    .hero-eyebrow {
      display:inline-flex; align-items:center; gap:8px;
      background:rgba(79,70,229,.1); color:var(--indigo);
      padding:6px 14px; border-radius:20px; font-size:.78rem; font-weight:600;
      letter-spacing:.05em; text-transform:uppercase; margin-bottom:20px;
    }
    .hero-eyebrow::before { content:''; width:7px; height:7px; border-radius:50%; background:var(--indigo); animation:pulse 2s infinite; }
    @keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:.5;transform:scale(1.4)} }
    .hero h1 { font-family:'Syne',sans-serif; font-size:clamp(2.2rem,4vw,3.4rem); font-weight:800; line-height:1.12; color:var(--slate); margin-bottom:22px; }
    .hero h1 em { font-style:normal; background:linear-gradient(90deg,var(--indigo),var(--violet)); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
    .hero-sub { font-size:1rem; color:var(--muted); max-width:480px; margin-bottom:36px; line-height:1.75; }
    .hero-actions { display:flex; gap:14px; flex-wrap:wrap; }
    .btn-primary { background:var(--indigo); color:var(--white); padding:14px 30px; border-radius:10px; font-size:.95rem; font-weight:600; text-decoration:none; box-shadow:0 4px 20px rgba(79,70,229,.35); transition:all .2s; }
    .btn-primary:hover { background:var(--violet); transform:translateY(-2px); }
    .btn-ghost { background:transparent; color:var(--slate); padding:14px 30px; border-radius:10px; border:1.5px solid var(--border); font-size:.95rem; font-weight:600; text-decoration:none; transition:all .2s; }
    .btn-ghost:hover { border-color:var(--indigo); color:var(--indigo); }

    /* Hero Chat Preview */
    .hero-visual { background:var(--white); border-radius:20px; box-shadow:0 20px 60px rgba(0,0,0,.10); padding:28px; border:1px solid var(--border); }
    .chat-header { display:flex; align-items:center; gap:10px; padding-bottom:16px; border-bottom:1px solid var(--border); margin-bottom:20px; }
    .chat-avatar { width:36px; height:36px; border-radius:10px; background:linear-gradient(135deg,var(--indigo),var(--violet)); display:flex; align-items:center; justify-content:center; font-size:1rem; }
    .chat-title { font-family:'Syne',sans-serif; font-weight:700; font-size:.9rem; }
    .chat-online { font-size:.75rem; color:var(--success); display:flex; align-items:center; gap:4px; }
    .chat-online::before { content:''; width:6px; height:6px; border-radius:50%; background:var(--success); display:block; }
    .bubble { padding:12px 16px; border-radius:12px; font-size:.85rem; line-height:1.55; margin-bottom:10px; max-width:88%; }
    .bubble-ai { background:var(--soft); color:var(--ink); border-bottom-left-radius:3px; }
    .bubble-user { background:var(--indigo); color:var(--white); margin-left:auto; border-bottom-right-radius:3px; }
    .typing { display:flex; gap:5px; padding:12px 16px; background:var(--soft); border-radius:12px; border-bottom-left-radius:3px; width:fit-content; }
    .typing span { width:7px; height:7px; background:var(--muted); border-radius:50%; animation:bounce 1.2s infinite; }
    .typing span:nth-child(2){animation-delay:.2s} .typing span:nth-child(3){animation-delay:.4s}
    @keyframes bounce{0%,100%{transform:translateY(0)}50%{transform:translateY(-5px)}}

    /* STATS */
    .stats-bar { display:grid; grid-template-columns:repeat(4,1fr); border-top:1px solid var(--border); border-bottom:1px solid var(--border); }
    .stat-item { text-align:center; padding:34px 20px; border-right:1px solid var(--border); }
    .stat-item:last-child { border-right:none; }
    .stat-num { font-family:'Syne',sans-serif; font-size:2.2rem; font-weight:800; color:var(--indigo); display:block; }
    .stat-label { font-size:.85rem; color:var(--muted); }
    @media(max-width:600px){ .stats-bar{grid-template-columns:repeat(2,1fr)} .stat-item:nth-child(2){border-right:none} }

    /* SECTIONS */
    section { padding:80px 6%; }
    .section-eyebrow { font-size:.78rem; font-weight:600; letter-spacing:.08em; text-transform:uppercase; color:var(--indigo); margin-bottom:14px; display:block; }
    .section-title { font-family:'Syne',sans-serif; font-size:clamp(1.8rem,3vw,2.4rem); font-weight:800; line-height:1.2; color:var(--slate); margin-bottom:16px; }
    .section-sub { font-size:1rem; color:var(--muted); max-width:560px; line-height:1.75; }
    .section-head { margin-bottom:50px; }
    .section-head.center { text-align:center; }
    .section-head.center .section-sub { margin:0 auto; }

    /* ===== AI AGENTS SECTION ===== */
    .agents-section { background: linear-gradient(180deg,#F9F8FF,#fff); }
    .agents-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(150px,1fr)); gap:12px; margin-bottom:28px; }
    .agent-btn {
      background:var(--white); border:1.5px solid var(--border); border-radius:14px;
      padding:20px 14px; cursor:pointer; text-align:left; transition:all .2s; position:relative;
    }
    .agent-btn:hover { border-color:rgba(79,70,229,.4); transform:translateY(-2px); box-shadow:0 8px 24px rgba(79,70,229,.1); }
    .agent-btn.active { border-color:var(--indigo); background:var(--soft); box-shadow:0 8px 28px rgba(79,70,229,.15); }
    .agent-btn .ab-icon { font-size:1.6rem; margin-bottom:10px; display:block; }
    .agent-btn .ab-name { font-family:'Syne',sans-serif; font-size:.88rem; font-weight:700; color:var(--slate); margin-bottom:4px; }
    .agent-btn .ab-hint { font-size:.75rem; color:var(--muted); line-height:1.4; }
    .live-dot { position:absolute; top:12px; right:12px; width:8px; height:8px; border-radius:50%; background:var(--success); }

    /* CHAT BOX */
    .chat-box {
      background:var(--white); border:1.5px solid var(--border); border-radius:20px;
      overflow:hidden; box-shadow:0 12px 40px rgba(0,0,0,.07);
    }
    .cb-topbar {
      display:flex; align-items:center; gap:12px; padding:16px 20px;
      background:var(--soft); border-bottom:1px solid var(--border);
    }
    .cb-avatar { width:40px; height:40px; border-radius:12px; background:linear-gradient(135deg,var(--indigo),var(--violet)); display:flex; align-items:center; justify-content:center; font-size:1.2rem; flex-shrink:0; }
    .cb-name { font-family:'Syne',sans-serif; font-weight:700; font-size:.95rem; color:var(--slate); }
    .cb-status { font-size:.75rem; color:var(--success); }
    .cb-clear { margin-left:auto; background:transparent; border:1px solid var(--border); border-radius:6px; padding:5px 12px; font-size:.78rem; color:var(--muted); cursor:pointer; }
    .cb-clear:hover { color:var(--ink); border-color:var(--ink); }

    .cb-messages { height:340px; overflow-y:auto; padding:20px; display:flex; flex-direction:column; gap:14px; }
    .cm { display:flex; gap:10px; align-items:flex-start; }
    .cm.user { flex-direction:row-reverse; }
    .cm-avatar { width:30px; height:30px; border-radius:50%; flex-shrink:0; display:flex; align-items:center; justify-content:center; font-size:.8rem; font-weight:600; }
    .cm.ai .cm-avatar { background:linear-gradient(135deg,var(--indigo),var(--violet)); color:#fff; }
    .cm.user .cm-avatar { background:#E5E7EB; color:var(--muted); font-size:.75rem; }
    .cm-bubble { max-width:80%; font-size:.88rem; line-height:1.65; padding:12px 16px; border-radius:16px; }
    .cm.ai .cm-bubble { background:var(--soft); color:var(--ink); border-bottom-left-radius:4px; }
    .cm.user .cm-bubble { background:var(--indigo); color:#fff; border-bottom-right-radius:4px; }

    .cb-suggestions { padding:0 20px 12px; display:flex; flex-wrap:wrap; gap:8px; }
    .cs { font-size:.78rem; padding:6px 14px; border-radius:20px; cursor:pointer; background:var(--white); border:1px solid var(--border); color:var(--muted); transition:all .15s; }
    .cs:hover { border-color:var(--indigo); color:var(--indigo); background:var(--soft); }

    .cb-input { display:flex; gap:10px; padding:16px 20px; border-top:1px solid var(--border); }
    .cb-input input { flex:1; border:1.5px solid var(--border); border-radius:10px; padding:11px 16px; font-size:.9rem; font-family:inherit; outline:none; transition:border-color .2s; }
    .cb-input input:focus { border-color:var(--indigo); }
    .cb-send { background:var(--indigo); color:#fff; border:none; border-radius:10px; padding:11px 22px; font-size:.9rem; font-weight:600; cursor:pointer; font-family:inherit; transition:background .2s; }
    .cb-send:hover { background:var(--violet); }
    .cb-send:disabled { opacity:.5; cursor:not-allowed; }

    /* HOW */
    .how { background:var(--white); }
    .steps { display:grid; grid-template-columns:repeat(3,1fr); gap:32px; }
    @media(max-width:700px){ .steps{grid-template-columns:1fr} }
    .step { text-align:center; padding:20px; }
    .step-num { width:64px; height:64px; border-radius:50%; background:linear-gradient(135deg,var(--indigo),var(--violet)); color:#fff; font-family:'Syne',sans-serif; font-size:1.4rem; font-weight:800; display:flex; align-items:center; justify-content:center; margin:0 auto 18px; box-shadow:0 6px 24px rgba(79,70,229,.3); }
    .step-title { font-family:'Syne',sans-serif; font-weight:700; font-size:1rem; color:var(--slate); margin-bottom:10px; }
    .step-desc { font-size:.87rem; color:var(--muted); line-height:1.7; }

    /* PRICING */
    .pricing { background:#F9F8FF; }
    .pricing-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:24px; }
    @media(max-width:800px){ .pricing-grid{grid-template-columns:1fr} }
    .plan { background:var(--white); border:1.5px solid var(--border); border-radius:18px; padding:36px 28px; position:relative; transition:transform .2s; }
    .plan:hover { transform:translateY(-3px); }
    .plan.popular { border-color:var(--indigo); box-shadow:0 12px 40px rgba(79,70,229,.16); }
    .popular-badge { position:absolute; top:-14px; left:50%; transform:translateX(-50%); background:linear-gradient(90deg,var(--indigo),var(--violet)); color:#fff; font-size:.72rem; font-weight:700; letter-spacing:.05em; padding:5px 16px; border-radius:20px; text-transform:uppercase; white-space:nowrap; }
    .plan-name { font-family:'Syne',sans-serif; font-size:1rem; font-weight:700; color:var(--muted); margin-bottom:12px; text-transform:uppercase; letter-spacing:.05em; }
    .plan-price { font-family:'Syne',sans-serif; font-size:2.6rem; font-weight:800; color:var(--slate); margin-bottom:6px; }
    .plan-price span { font-size:1rem; font-weight:400; color:var(--muted); }
    .plan-desc { font-size:.85rem; color:var(--muted); margin-bottom:28px; }
    .plan-features { list-style:none; margin-bottom:32px; display:flex; flex-direction:column; gap:12px; }
    .plan-features li { font-size:.88rem; color:var(--ink); display:flex; align-items:center; gap:10px; }
    .plan-features li::before { content:'✓'; width:20px; height:20px; background:rgba(79,70,229,.1); color:var(--indigo); border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:.72rem; font-weight:700; flex-shrink:0; }
    .plan-btn { display:block; text-align:center; padding:13px; border-radius:10px; font-size:.9rem; font-weight:600; text-decoration:none; transition:all .2s; cursor:pointer; font-family:inherit; border:none; }
    .plan-btn.outline { border:1.5px solid var(--indigo); color:var(--indigo); background:transparent; }
    .plan-btn.outline:hover { background:var(--indigo); color:#fff; }
    .plan-btn.filled { background:var(--indigo); color:#fff; box-shadow:0 4px 18px rgba(79,70,229,.3); }
    .plan-btn.filled:hover { background:var(--violet); }

    /* CTA BANNER */
    .cta-wrap { padding:0 6% 80px; }
    .cta-banner { background:linear-gradient(135deg,var(--slate),#2D1B69); border-radius:24px; padding:72px 8%; text-align:center; }
    .cta-banner h2 { font-family:'Syne',sans-serif; font-size:clamp(1.8rem,3vw,2.4rem); font-weight:800; color:#fff; margin-bottom:16px; }
    .cta-banner p { font-size:1rem; color:rgba(255,255,255,.7); max-width:500px; margin:0 auto 36px; line-height:1.75; }
    .cta-actions { display:flex; gap:14px; justify-content:center; flex-wrap:wrap; }
    .btn-light { background:#fff; color:var(--indigo); padding:14px 32px; border-radius:10px; font-size:.95rem; font-weight:700; text-decoration:none; transition:transform .15s; }
    .btn-light:hover { transform:translateY(-2px); }
    .btn-ol { background:transparent; color:#fff; border:1.5px solid rgba(255,255,255,.4); padding:14px 32px; border-radius:10px; font-size:.95rem; font-weight:600; text-decoration:none; transition:all .2s; }
    .btn-ol:hover { border-color:#fff; }

    /* FOOTER */
    footer { background:var(--slate); color:rgba(255,255,255,.6); padding:60px 6% 30px; }
    .footer-grid { display:grid; grid-template-columns:2fr 1fr 1fr 1fr; gap:40px; margin-bottom:48px; }
    @media(max-width:700px){ .footer-grid{grid-template-columns:1fr 1fr} }
    .footer-brand p { font-size:.88rem; margin-top:14px; line-height:1.7; max-width:260px; }
    .footer-col h4 { font-family:'Syne',sans-serif; font-size:.85rem; font-weight:700; color:#fff; margin-bottom:16px; letter-spacing:.05em; text-transform:uppercase; }
    .footer-col ul { list-style:none; display:flex; flex-direction:column; gap:10px; }
    .footer-col a { font-size:.87rem; color:rgba(255,255,255,.55); text-decoration:none; transition:color .2s; }
    .footer-col a:hover { color:#fff; }
    .footer-bottom { border-top:1px solid rgba(255,255,255,.1); padding-top:24px; display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:10px; font-size:.82rem; }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo">Vertex<span>AI</span></a>
  <ul class="nav-links">
    <li><a href="#agents">AI Agents</a></li>
    <li><a href="#how">How it Works</a></li>
    <li><a href="#pricing">Pricing</a></li>
  </ul>
  <a href="#agents" class="nav-cta">Try Free →</a>
</nav>

<!-- HERO -->
<section class="hero">
  <div>
    <div class="hero-eyebrow">AI Vertical Employee Platform</div>
    <h1>Your industry's<br/><em>smartest employee</em><br/>never sleeps.</h1>
    <p class="hero-sub">Purpose-built AI assistants for immigration, accounting, real estate, insurance & legal. Talk to them — right now, below.</p>
    <div class="hero-actions">
      <a href="#agents" class="btn-primary">Talk to an AI Agent →</a>
      <a href="#how" class="btn-ghost">How it works</a>
    </div>
  </div>
  <div class="hero-visual">
    <div class="chat-header">
      <div class="chat-avatar">🛂</div>
      <div>
        <div class="chat-title">Immigration AI</div>
        <div class="chat-online">Online — ready to help</div>
      </div>
    </div>
    <div class="bubble bubble-user">What docs does my client need for H-1B?</div>
    <div class="bubble bubble-ai">You'll need <strong>Form I-129</strong>, a Labor Condition Application, employer support letter, degree certificates with evaluation, and evidence of specialty occupation. Want me to draft the employer letter?</div>
    <div class="bubble bubble-user">Yes please!</div>
    <div class="typing"><span></span><span></span><span></span></div>
  </div>
</section>

<!-- STATS -->
<div class="stats-bar">
  <div class="stat-item"><span class="stat-num">5+</span><span class="stat-label">Industry Verticals</span></div>
  <div class="stat-item"><span class="stat-num">10×</span><span class="stat-label">Faster Document Work</span></div>
  <div class="stat-item"><span class="stat-num">24/7</span><span class="stat-label">Always Available</span></div>
  <div class="stat-item"><span class="stat-num">98%</span><span class="stat-label">Client Satisfaction</span></div>
</div>

<!-- ===== LIVE AI AGENTS ===== -->
<section class="agents-section" id="agents">
  <div class="section-head">
    <span class="section-eyebrow">Live AI Agents</span>
    <h2 class="section-title">Pick your industry.<br/>Start chatting instantly.</h2>
    <p class="section-sub">Each agent is a real AI trained deep on their field. Ask anything — they'll answer like a specialist.</p>
  </div>

  <div class="agents-grid" id="agentsGrid"></div>

  <div class="chat-box">
    <div class="cb-topbar">
      <div class="cb-avatar" id="cbAvatar">🛂</div>
      <div>
        <div class="cb-name" id="cbName">Immigration AI</div>
        <div class="cb-status">● Online — ready to help you</div>
      </div>
      <button class="cb-clear" onclick="clearChat()">Clear chat</button>
    </div>
    <div class="cb-messages" id="cbMessages"></div>
    <div class="cb-suggestions" id="cbSuggestions"></div>
    <div class="cb-input">
      <input type="text" id="cbInput" placeholder="Ask your AI agent anything..." onkeydown="if(event.key==='Enter') sendMsg()"/>
      <button class="cb-send" id="cbSend" onclick="sendMsg()">Send →</button>
    </div>
  </div>
</section>

<!-- HOW IT WORKS -->
<section class="how" id="how">
  <div class="section-head center">
    <span class="section-eyebrow">How It Works</span>
    <h2 class="section-title">Up and running in three steps.</h2>
    <p class="section-sub">No engineers needed. Pick your industry, configure, and your AI employee starts working immediately.</p>
  </div>
  <div class="steps">
    <div class="step">
      <div class="step-num">1</div>
      <div class="step-title">Choose Your Vertical</div>
      <p class="step-desc">Select the industry that matches your practice. Each vertical is pre-trained on real domain knowledge, regulations, and workflows.</p>
    </div>
    <div class="step">
      <div class="step-num">2</div>
      <div class="step-title">Configure & Connect</div>
      <p class="step-desc">Link your documents and CRM in minutes. Set your preferences — tone, language, response style — so the AI matches your brand.</p>
    </div>
    <div class="step">
      <div class="step-num">3</div>
      <div class="step-title">Deploy & Delegate</div>
      <p class="step-desc">Your AI employee starts handling queries, drafting documents, and assisting clients immediately — while you focus on higher-value work.</p>
    </div>
  </div>
</section>

<!-- PRICING -->
<section class="pricing" id="pricing">
  <div class="section-head center">
    <span class="section-eyebrow">Pricing</span>
    <h2 class="section-title">Simple, transparent pricing.</h2>
    <p class="section-sub">Start free, scale as you grow. No hidden fees.</p>
  </div>
  <div class="pricing-grid">
    <div class="plan">
      <div class="plan-name">Starter</div>
      <div class="plan-price">$0 <span>/ month</span></div>
      <p class="plan-desc">Perfect for solo professionals testing the platform.</p>
      <ul class="plan-features">
        <li>1 AI vertical</li>
        <li>50 queries / month</li>
        <li>Basic document drafting</li>
        <li>Email support</li>
      </ul>
      <button class="plan-btn outline">Start Free</button>
    </div>
    <div class="plan popular">
      <div class="popular-badge">Most Popular</div>
      <div class="plan-name">Professional</div>
      <div class="plan-price">$49 <span>/ month</span></div>
      <p class="plan-desc">For growing practices that need a full AI team member.</p>
      <ul class="plan-features">
        <li>3 AI verticals</li>
        <li>Unlimited queries</li>
        <li>Document review & drafting</li>
        <li>Client-facing chat embed</li>
        <li>Deadline alerts</li>
        <li>Priority support</li>
      </ul>
      <button class="plan-btn filled">Get Started</button>
    </div>
    <div class="plan">
      <div class="plan-name">Enterprise</div>
      <div class="plan-price">Custom</div>
      <p class="plan-desc">For firms that need custom training and integrations.</p>
      <ul class="plan-features">
        <li>All verticals</li>
        <li>Custom AI on your data</li>
        <li>CRM / ERP integrations</li>
        <li>SSO & audit logs</li>
        <li>Dedicated account manager</li>
      </ul>
      <button class="plan-btn outline">Contact Sales</button>
    </div>
  </div>
</section>

<!-- CTA -->
<div class="cta-wrap">
  <div class="cta-banner">
    <h2>Ready to hire your AI employee?</h2>
    <p>Join professionals across 5+ industries saving time, reducing errors, and growing with VertexAI.</p>
    <div class="cta-actions">
      <a href="#agents" class="btn-light">Try an AI Agent Free</a>
      <a href="#how" class="btn-ol">See How It Works</a>
    </div>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <div class="footer-grid">
    <div class="footer-brand">
      <a href="#" class="nav-logo" style="color:#fff">Vertex<span style="color:#A78BFA">AI</span></a>
      <p>Purpose-built AI employees for professionals who can't afford to waste time on tasks a machine can do better.</p>
    </div>
    <div class="footer-col">
      <h4>Industries</h4>
      <ul>
        <li><a href="#">Immigration</a></li>
        <li><a href="#">Accounting</a></li>
        <li><a href="#">Real Estate</a></li>
        <li><a href="#">Insurance</a></li>
        <li><a href="#">Legal Review</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Product</h4>
      <ul>
        <li><a href="#">Features</a></li>
        <li><a href="#">Pricing</a></li>
        <li><a href="#">Security</a></li>
        <li><a href="#">Changelog</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Company</h4>
      <ul>
        <li><a href="#">About</a></li>
        <li><a href="#">Blog</a></li>
        <li><a href="#">Careers</a></li>
        <li><a href="#">Contact</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <span>© 2025 VertexAI. All rights reserved.</span>
    <span>Made with ❤️ for professionals everywhere.</span>
  </div>
</footer>

<script>
const AGENTS = [
  {
    id:"immigration", icon:"🛂", name:"Immigration AI", hint:"Visas, H-1B, green cards",
    system:`You are an expert AI immigration consultant assistant. You help immigration consultants and lawyers with visa eligibility, H-1B and green card petitions, document checklists, case deadlines, and immigration law questions. Be precise, reference relevant forms and regulations (like Form I-129, I-485, etc.), and offer to draft documents when helpful. Keep answers clear and professional. You work for VertexAI.`,
    suggestions:["What docs for H-1B petition?","Explain green card process","Draft an employer support letter","H-1B vs O-1 visa difference"]
  },
  {
    id:"accounting", icon:"📊", name:"Accounting AI", hint:"Tax, GST, audits, compliance",
    system:`You are an expert AI accounting assistant. You help accountants and finance professionals with tax computations, GST/VAT, income tax, ledger reconciliation, financial reporting, deductions, and compliance. Provide clear, accurate advice referencing relevant tax laws. Offer to generate reports or do calculations when asked. You work for VertexAI.`,
    suggestions:["Calculate GST on ₹50,000","What deductions can I claim?","Explain ITR filing steps","Difference: sole proprietor vs LLP tax"]
  },
  {
    id:"realestate", icon:"🏠", name:"Real Estate AI", hint:"Listings, comps, contracts",
    system:`You are an expert AI real estate assistant. You help brokers and agents with property listings, market analysis, comparable sales (comps), buyer qualification, contract drafting, and client communication. Be practical and results-focused. Offer to draft listing descriptions, client emails, or sale agreements. You work for VertexAI.`,
    suggestions:["Draft a property listing","How to qualify a buyer?","What are market comps?","Write a follow-up email to a lead"]
  },
  {
    id:"insurance", icon:"🛡️", name:"Insurance AI", hint:"Policies, claims, quotes",
    system:`You are an expert AI insurance assistant. You help insurance agents compare policies, explain coverage in plain language, assist with claim procedures, generate quotes guidance, and handle renewals. Be clear and client-friendly. Help agents explain complex terms simply to customers. You work for VertexAI.`,
    suggestions:["Compare term vs whole life","How to file a claim?","Explain liability coverage","What is no-claim bonus?"]
  },
  {
    id:"legal", icon:"⚖️", name:"Legal Review AI", hint:"Contracts, risk, compliance",
    system:`You are an expert AI legal document review assistant. You help lawyers and legal teams review contracts, flag risky or unusual clauses, summarize agreements, extract key obligations and deadlines, and check for regulatory compliance issues. Be precise and structured. Clearly flag any red-flag terms. You work for VertexAI.`,
    suggestions:["What clauses should I watch for?","Summarize a typical NDA","Explain indemnity clause","What makes a contract unenforceable?"]
  }
];

let activeId = "immigration";
let convos = {};
let loading = false;
AGENTS.forEach(a => convos[a.id] = []);

function getAgent(id){ return AGENTS.find(a=>a.id===id); }

function renderAgentBtns(){
  document.getElementById("agentsGrid").innerHTML = AGENTS.map(a=>`
    <button class="agent-btn ${a.id===activeId?'active':''}" onclick="selectAgent('${a.id}')">
      <div class="live-dot"></div>
      <span class="ab-icon">${a.icon}</span>
      <div class="ab-name">${a.name}</div>
      <div class="ab-hint">${a.hint}</div>
    </button>`).join("");
}

function renderTopbar(){
  const a = getAgent(activeId);
  document.getElementById("cbAvatar").textContent = a.icon;
  document.getElementById("cbName").textContent = a.name;
}

function renderMessages(){
  const a = getAgent(activeId);
  const msgs = convos[activeId];
  const list = document.getElementById("cbMessages");
  if(msgs.length===0){
    list.innerHTML = `<div class="cm ai">
      <div class="cm-avatar">${a.icon}</div>
      <div class="cm-bubble">Hi! I'm your <strong>${a.name}</strong>. I specialise in ${a.hint.toLowerCase()}. Ask me anything — I'm here to help!</div>
    </div>`;
  } else {
    list.innerHTML = msgs.map(m=>`
      <div class="cm ${m.role}">
        <div class="cm-avatar">${m.role==='ai'?a.icon:'You'[0]}</div>
        <div class="cm-bubble">${m.text.replace(/\n/g,'<br/>')}</div>
      </div>`).join("");
  }
  list.scrollTop = list.scrollHeight;
}

function renderSuggestions(){
  const a = getAgent(activeId);
  document.getElementById("cbSuggestions").innerHTML = a.suggestions.map(s=>
    `<span class="cs" onclick="useSuggestion('${s.replace(/'/g,"\\'")}')">✦ ${s}</span>`
  ).join("");
}

function selectAgent(id){
  activeId = id;
  renderAgentBtns();
  renderTopbar();
  renderMessages();
  renderSuggestions();
  document.getElementById("cbInput").placeholder = `Ask your ${getAgent(id).name} anything...`;
}

function clearChat(){
  convos[activeId] = [];
  renderMessages();
}

function useSuggestion(text){
  document.getElementById("cbInput").value = text;
  sendMsg();
}

async function sendMsg(){
  if(loading) return;
  const input = document.getElementById("cbInput");
  const text = input.value.trim();
  if(!text) return;
  input.value = "";
  loading = true;
  document.getElementById("cbSend").disabled = true;

  const agent = getAgent(activeId);
  convos[activeId].push({role:"user", text});

  const list = document.getElementById("cbMessages");
  list.innerHTML += `
    <div class="cm user">
      <div class="cm-avatar">Y</div>
      <div class="cm-bubble">${text}</div>
    </div>
    <div class="cm ai" id="typingMsg">
      <div class="cm-avatar">${agent.icon}</div>
      <div class="cm-bubble" style="display:flex;gap:5px;align-items:center;padding:14px 16px">
        <span style="width:7px;height:7px;border-radius:50%;background:#6B7280;animation:bounce 1.2s infinite"></span>
        <span style="width:7px;height:7px;border-radius:50%;background:#6B7280;animation:bounce 1.2s .2s infinite"></span>
        <span style="width:7px;height:7px;border-radius:50%;background:#6B7280;animation:bounce 1.2s .4s infinite"></span>
      </div>
    </div>`;
  list.scrollTop = list.scrollHeight;

  try {
    const messages = convos[activeId].map(m=>({
      role: m.role==="ai"?"assistant":"user",
      content: m.text
    }));

    const res = await fetch("https://api.anthropic.com/v1/messages",{
      method:"POST",
      headers:{"Content-Type":"application/json"},
      body: JSON.stringify({
        model:"claude-sonnet-4-20250514",
        max_tokens:1000,
        system: agent.system,
        messages
      })
    });

    const data = await res.json();
    const reply = data.content?.map(c=>c.text||"").join("") || "Sorry, I couldn't get a response. Please try again.";
    convos[activeId].push({role:"ai", text:reply});

    const typingEl = document.getElementById("typingMsg");
    if(typingEl){
      typingEl.querySelector(".cm-bubble").innerHTML = reply.replace(/\n/g,"<br/>");
      typingEl.removeAttribute("id");
    }
  } catch(e) {
    const typingEl = document.getElementById("typingMsg");
    if(typingEl) typingEl.querySelector(".cm-bubble").textContent = "Connection error — please try again.";
  }

  loading = false;
  document.getElementById("cbSend").disabled = false;
  list.scrollTop = list.scrollHeight;
}

// Init
renderAgentBtns();
renderTopbar();
renderMessages();
renderSuggestions();
</script>
</body>
</html>
